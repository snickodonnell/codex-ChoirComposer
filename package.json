{
  "name": "choir-composer-frontend-tools",
  "private": true,
  "version": "1.0.0",
  "description": "Frontend tooling dependencies for browser-side PDF export",
  "license": "UNLICENSED",
  "dependencies": {
    "canvg": "^4.0.3",
    "jspdf": "^2.5.2"
  },
  "scripts": {
    "bundle:frontend-deps": "node scripts_bundle_frontend_deps.mjs",
    "postinstall": "npm run bundle:frontend-deps"
  },
  "devDependencies": {
    "esbuild": "^0.25.10"
  }
}
